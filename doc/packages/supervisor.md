# supervisor

You need supervisor or something similar to run multi-component images because docker images are meant to run a process with its own dependencies, and not be an actual machine in itself.
